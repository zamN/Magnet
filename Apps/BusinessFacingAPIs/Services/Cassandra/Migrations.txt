# CREATE KEYSPACE
CREATE KEYSPACE BusinessFacing WITH REPLICATION = { 'class' : 'NetworkTopologyStrategy', 'datacenter1': 3 } AND DURABLE_WRITES = true;

# CREATE TABLE
CREATE TABLE BusinessFacing.accounts (
name varchar PRIMARY KEY,
email varchar,
city varchar,
state varchar,
zip int,
business_type varchar,
birthday timestamp
);
CREATE TABLE BusinessFacing.accounts ( name varchar PRIMARY KEY, email varchar, city varchar, state varchar, zip int, business_type varchar, birthday timestamp );
